# Prompts Used

| # | Prompt | Model |
|---|--------|-------|
| 1 | "Inside project-b-ai-assisted folder, create [structure]... Build a URL Shortener: a small application that takes long URLs, generates short codes, and redirects users. It also tracks how many times each short link has been clicked. [Full requirements...]" | Cursor (Claude / Auto) |

## Model Details

- **Cursor** — Used for initial scaffold and full implementation (backend, frontend, tests, docs)
- **Claude / Auto** — Cursor's default model for coding tasks
